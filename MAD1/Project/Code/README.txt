This project works with python3. Install python3 and verify it is accessible. All these steps can be done by opening the project folder using VS code. Once python is installed follow the below steps.

Create virtual environment
python  -m venv .project

Activate it
.project/Scripts/activate

Intall necessary modules

pip install --upgrade pip
pip install -r requirements.txt


Run the application
python app.py